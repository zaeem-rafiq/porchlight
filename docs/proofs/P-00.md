# P-00 proof

`PROOF P-00: aws=PASS bedrock=PASS agentcore=PASS supabase=PASS twilioA=PASS(SMda) twilioB=PASS(SM40) nws=PASS calendar=WARN allowlist_guard=PASS`

- `scripts/preflight.py` exit 0; owner phone shows two texts, one per Twilio number.
- Calendar is WARN-only by contract (no refresh token configured).
- Bedrock runs Sonnet 4.5 via inference profile (Sonnet 5 not granted on this account).
- Guard unit tests: `pytest tests/test_safety.py -q` → 2 passed.

## Walkthrough (5 lines)

1. Empty folder became a repo: skeleton, rules, docs, venv, first commits.
2. `.env` filled by owner; AWS via login, Bedrock 4.5 profile, Supabase schema exposed.
3. Preflight proves every dependency plus the allowlist guard in one exit-0 gate.
4. Two Twilio channels confirmed by real texts to the owner phone.
5. Next: HAC-25 roster, protocol, resources, and alert fixtures.
