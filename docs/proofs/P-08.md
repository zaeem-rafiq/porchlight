# P-08 proof

`PROOF P-08: reset_demo ok runtime=READY lambdas=3/3 console=200 telegram=ok = PASS`
`PROOF P-08: rehearsal — Ruth text received; "1" → ok; Alvarez medical → coordinator text received; reply 1 → volunteer Y → dispatch + calendar; timestamps printed; zero sends outside the allowlist = PASS`
`PROOF P-08: architecture diagram docs/architecture.mmd rendered to docs/architecture.svg and docs/media/architecture.png = PASS`
`PROOF P-08: full suite 141 tests passed, markdownlint clean, code freeze declared (freeze=PASS, voice=skipped) = PASS`

- Reset script `scripts/reset_demo.py` closes open hazard events, archives old contacts/dispatches/escalations, and reseeds 40 synthetic residents, 5 volunteers, 3 resources, and protocol v1 under strict `PHONE_ALLOWLIST` assertions.
- Health probes verified: AgentCore Runtime status `READY`, AWS Lambdas `3/3` (`NwsPoll`, `TelegramInbound` with live Function URL, `Tick`), Next.js standalone Console `HTTP 200`, and Telegram Bot API `getMe` reachable (`@porchlight_checkon_bot`).
- Automated dress rehearsal `scripts/rehearsal_p08.py` executed full end-to-end lifecycle: alert injection (`heat.json`), wave outreach to Ruth Alvarez (`sent` in 11.2s), Ruth "1" wellness check (`ok` in 0.9s), Alvarez medical distress ("AC broke, dizzy" in 23.4s) held by Strands `BeforeToolCallEvent` hook with coordinator Telegram ping dispatched, coordinator reply "1" approval, volunteer Marcus Webb acceptance ("Y"), dispatch accepted + calendar logged (`calendar=skipped`), and strict assertion of zero sends outside the allowlist.
- Full system architecture diagram authored in Mermaid (`docs/architecture.mmd`) and compiled to vector SVG (`docs/architecture.svg`) and high-resolution media PNG (`docs/media/architecture.png`).
- Comprehensive `README.md` and `docs/runbook-demo.md` delivered with problem framing, architecture, safety principles, Strands features, AgentCore services, benchmark evals table (27/26/30/29), quickstart, deployment steps, contingency runbook, and zero markdownlint errors.
- Test suite passes cleanly: 138 passed, 3 skipped across 141 tests.

## Walkthrough (5 lines)

1. `reset_demo.py` cleared active events, reseeded 40 residents, and verified runtime, lambdas, console, and Telegram bot.
2. `rehearsal_p08.py` ran end-to-end: Ruth received outreach, replied "1" to confirm wellness, and transitioned to `ok`.
3. Alvarez distress ("AC broke, dizzy") triaged to `medical`, held by Strands Coordinator Gate, and pinged the coordinator.
4. Coordinator approved option 1, volunteer Marcus Webb accepted via "Y", and dispatch was logged with zero allowlist violations.
5. Architecture diagram rendered, comprehensive README finalized with zero markdownlint warnings, and code freeze declared (`freeze=PASS`, `voice=skipped`).
