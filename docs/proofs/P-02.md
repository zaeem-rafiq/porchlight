# P-02 proof

`PROOF P-02: live poll DEMO_ZONE → HTTP 200, 2 alerts parsed (N may be 0) = PASS`
`PROOF P-02: inject heat.json → hazard_events row created once (second inject de-duplicated) = PASS`
`PROOF P-02: tiering 40 residents → tier1=15 tier2=10 tier3=15; oxygen-concentrator resident tier1 under outage.json; reasons present for every boundary case; wall time <90s = PASS`

- Live poll found 2 real alerts for ARZ001, stored `observed_only` (LIVE_ZONE never set).
- Inject runs: `created=1 dupes=0` then `created=0 dupes=1` (dedupe by NWS id).
- Heat tiering 38.3s; outage tiering 39.0s with tier1=16 tier2=8 tier3=16, both oxygen users tier1.
- `pytest tests/` 7 passed. Strands APIs verified against installed package before use.

## Walkthrough (5 lines)

1. Poller fetches NWS with User-Agent, matches both heat-warning names, stores observed-only.
2. Same path ingests drill fixtures with NWS-id dedupe for demos.
3. Deterministic points score every resident; agent adjudicates boundary cases with reasons.
4. Models live in agent/models.py, now protected (ADR for changes).
5. Next: HAC-27 outreach wave plus per-resident conversation agents.
