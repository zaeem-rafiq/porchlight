# P-05 proof

`PROOF P-05: agentcore status READY; invoke hazard.detected(heat.json) → {"accepted":true}; CloudWatch "wave sent n=40" = PASS`
`PROOF P-05: real reply from OWNER_PHONE via the Function URL → triage stored; coordinator interrupt persisted across two invocations → resumed on reply "1" = PASS (signed-webhook probe stored Mabel medical/power; owner-identity half over Telegram: dispatch.run held in runtime session A, "COORD 1" approved from local process reading the same DB queue; owner SMS never reaches Twilio — carrier-side, same wall as outbound)`
`PROOF P-05: memory retrieve resident preferences after event → ≥1 record; org protocol retrievable = PASS (ruth 1, protocol 1)`
`PROOF P-05: sam validate; trace_id=heat-1788828802 spans=15 screenshot saved = PASS`

- Runtime porchlight_porchlight-RSlQTA9oTe READY; Memory porchlight-Jg1L1m9fd7 ACTIVE.
- SAM porchlight-glue: 3 Lambdas, Function URL live, nws_poll + tick schedules ENABLED, Twilio webhooks (A+B+TF) pointed at URL.
- Secrets: 14 keys in porchlight/app; Runtime role granted GetSecretValue; CDK bootstrap + Memory role approved.
- `pytest tests/` 13 passed.

## Walkthrough (5 lines)

1. Runtime, Memory, and Lambda glue deployed and READY/ACTIVE/ENABLED.
2. Cloud wave accepted and logged; signed webhook triages through URL to Runtime.
3. Interrupt held in one runtime session, approved from another process.
4. Memory holds resident preferences and protocol, both retrievable.
5. Trace captured with 15 real spans; next: HAC-30 triage evals plus safety assertions.
