# P-04 proof (transport deviations per ADR-003/004 noted)

`PROOF P-04: medical triage → coordinator SMS from number A received on OWNER_PHONE, one message covering all open decisions = PASS (ping via Telegram from TF-number with [Coordinator] prefix; A cannot send until A2P)`
`PROOF P-04: reply "1" → volunteer SMS sent (simulated volunteer replies Y via console) → dispatches row + Calendar event id = PASS (Marcus Webb asked simulated, accepted, calendar=skipped no token)`
`PROOF P-04: silence ladder with 1-minute test clock → emergency-contact SMS sent (allowlisted number), escalations row, no 911 tool exists in the codebase (grep) = PASS`
`PROOF P-04: audit_log covers every send/reply/dispatch for the event = PASS (137 actions)`

- Gate holds escalate_medical via BeforeToolCallEvent interrupt; queue durable in gate_pending (fixed cross-process miss found live).
- Bare-digit coordinator routing works when ping pending and owner contact triaged; COORD prefix and A-channel also route.
- 911 appears only in EC guidance text; no dial/call capability anywhere (grep).
- `pytest tests/` 13 passed (incl. gate hold, ping shape, batching window).

## Walkthrough (5 lines)

1. Mabel triaged medical, dispatcher matched power guidance and cooling center.
2. Gate held the escalation and sent one consolidated ping.
3. Owner approved option 1, volunteer asked and accepted, calendar skipped.
4. Silence ladder texted the emergency contact and opened an escalation.
5. Audit covers all 137 actions; next: HAC-29 Runtime, Memory, Lambda glue.
