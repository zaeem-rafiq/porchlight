# P-01 proof

`PROOF P-01: residents=40 volunteers=5 resources=3 protocol=1 fixtures=2 = PASS`
`PROOF P-01: allowlist check — 40/40 resident phones allowlisted, 1 real (OWNER_PHONE) = PASS`
`PROOF P-01: seed --reset twice → identical row counts = PASS`

(Console renders ASCII variants `->`/`-`; semantics identical. `scripts/seed.py --reset --verify-twice` exit 0; `pytest tests/` 5 passed.)

- Migration `db/migrations/001_porchlight_schema.sql` applied via pooled `SUPABASE_DB_URL` (9 tables + grants).
- Ruth Alvarez = OWNER_PHONE; 84 fictional 555-01XX placeholders appended to PHONE_ALLOWLIST (87 entries, append-only).
- Eval anchors present: oxygen-concentrator users, call-only residents, "fine"-to-everything Mabel.

## Walkthrough (5 lines)

1. Nine tables migrated with REST grants; schema already REST-exposed.
2. Forty synthetic residents seeded, Ruth first with the real number.
3. Five volunteers, three cooling centers, one protocol row, two alert fixtures.
4. Reset run twice proves identical counts; allowlist proves 40/40, 1 real.
5. Next: HAC-26 NWS poller plus tiering agent.
