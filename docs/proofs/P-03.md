# P-03 proof (transport: Telegram per ADR-004; SMS re-proof pending TFV)

`PROOF P-03: inject heat.json → tier-1 wave sent, OWNER_PHONE (Ruth) receives the text within 60s, 40 contacts rows status=sent = PASS (Telegram delivery; SMS blocked by 30034/30032)`
`PROOF P-03: reply "1" from OWNER_PHONE → Ruth status=ok without a model call = PASS`
`PROOF P-03: reply "AC broke yesterday and I feel dizzy" → status=medical need=cooling quote set = PASS`
`PROOF P-03: simulated inbound for 3 placeholder residents → 3 triaged; retry ladder fires for a silent resident with a 1-minute test clock = PASS`
`PROOF P-03: STOP from OWNER_PHONE → opted_out=true, no further sends = PASS`

- Wave: sent=40 real=1 simulated=39, 40 contact rows. Re-wave after STOP: sent=39 real=0 (owner suppressed).
- Simulated: Elena es→ok, Walter "2"→needs_help/other short-circuit, Mabel flickering→medical/power. Ladder resend at +1min, unreachable at +2min, applied to one silent contact.
- `pytest tests/` 8 passed (incl. short-circuit, ladder, copy, session ids).
- Known nits: triage agent once invented `unreachable` for garbage input (P-06 evals to pin statuses); Spanish template re-reviewed per process (nondeterministic wording, slots intact).

## Walkthrough (5 lines)

1. Tiered wave went out with plain-words copy, STOP line on first contact.
2. Owner replies triaged live: 1 to OK with no model call, dizzy text to medical.
3. Placeholders answered through the same handler via the simulated path.
4. Silent resident climbed the ladder on a compressed clock; STOP ended all sends.
5. Next: HAC-28 dispatcher plus the one consolidated coordinator ping.
