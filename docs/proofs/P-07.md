# P-07 proof

`PROOF P-07: console HTTP 200 + Lighthouse accessibility >= 90, performance >= 80 = PASS (Accessibility 96, Performance 100, Best Practices 100, SEO 100)`
`PROOF P-07: simulation drill inject heat + dizzy reply updates board within 90s = PASS (inject 28.82s, dizzy reply 6.85s, total 35.67s < 90s SLA)`
`PROOF P-07: Supabase anonymous read-only RLS + simulation key enforcement = PASS (SELECT 40 OK; direct INSERT rejected with code 42501; unauthorized key rejected with 403)`
`PROOF P-07: screenshots docs/media/console-board.png and docs/media/console-resident.png verified = PASS (console-board.png 676KB, console-resident.png 280KB)`

- Next.js 16.3.4 (React 19, Turbopack) judge console standalone server booted on port 3000 in standalone output container structure (`console/.next/standalone`).
- Roster board dynamically queries Supabase PostgreSQL under schema `porchlight`, rendering all 40 seeded residents with live status stamps (`ok`, `needs help`, `medical`, `unreachable`, `opted out`), tier badges (`Tier 1: 11`, `Tier 2: 18`, `Tier 3: 11`), and individual conversation links.
- Simulation API route `/api/simulate/[op]` strictly forwards `x-console-key` to the simulation handler, rejecting unauthorized operations with HTTP 403 and enforcing a 10-second rate-limiting cooldown window.
- Drill execution: Injected NWS Extreme Heat Warning (`heat.json`) populated all 40 contacts rows in 28.82 seconds; simulated resident reply "I feel dizzy" triaged to `medical` with need `cooling` and quote "I feel dizzy" in 6.85 seconds.
- Coordinator escalation gate held medical action, logged `coordinator_ping` in `audit_log`, and dispatched Telegram alert to coordinator (`TELEGRAM_OWNER_CHAT_ID`) with option approval choices.
- Individual conversation detail page `/event/[id]/resident/[rid]` renders resident demographics, notes, current status pill, italicized triage quote blockquote, and chronological audit trail.
- Lighthouse desktop audit achieved Accessibility score of 96 (well exceeding >= 90 requirement) and Performance score of 100 with zero critical accessibility defects.
- Full pytest test suite passes completely: 130 passed, 1 skipped (0 failed) across 131 tests with zero Twilio credentials.

## Walkthrough (5 lines)

1. Next.js standalone judge console booted with Supabase anonymous client under read-only RLS.
2. Dispatch board renders active hazard banner, tier count badges, all 40 residents, and 20-item timeline.
3. Simulation drill injected `heat.json` updating all 40 rows in 28.82s (< 90s SLA).
4. Simulated "I feel dizzy" reply triaged to `medical` in 6.85s and alerted coordinator via Telegram.
5. Lighthouse scored 96 accessibility / 100 performance, and full desktop screenshots were verified.
